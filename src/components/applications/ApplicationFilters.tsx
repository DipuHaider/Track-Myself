export default function ApplicationFilters() {
  return (
    <section className="surface rounded-lg border p-4">
      <p className="text-muted text-sm">Filter controls scaffold (status/platform/date).</p>
    </section>
  );
}
