export const metadata = {
  title: "Create a free account",
  description:
    "Create a free TrackMyself account to track job applications through ten pipeline stages, catch ghost listings, and export ATS, Europass and Designer CVs as Word documents.",
  alternates: { canonical: "/register" },
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
